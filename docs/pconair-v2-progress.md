# PConAir v2 — Execution Progress

> Working state for the v2 build (plan: `docs/pconair-v2-plan.md`). Updated at every phase boundary, **before** any context compaction. If you are resuming from a compacted context, read this file and the plan first.

## Context rules (token budget)
- **Never read `Google-Slides-Controller/main.js` whole** (13,195 lines ≈ a full context window). Grep for the feature, read only that section.
- Compact at phase boundaries only, never mid-phase. Update this file first.
- Baseline established 2026-06-10: `npx vitest run` → **202 tests passing, 16 files**.
- node_modules was rebuilt for Linux (repo synced from a Mac); if rollup native module errors appear, `rm -rf node_modules && npm ci`.

## Phase status

| Phase | Status | Notes |
|---|---|---|
| 1 — Tray shell, settings window, /remote, port config | ✅ done 2026-06-10 | New: `src/main/app-settings.ts` (port in userData `app-settings.json`, env > file > 8080), `src/main/tray.ts`, `src/main/settings-window.ts` (+ `src/renderer/settings/`, `settings-preload.ts`, IPC `pconair:settings:*`), `/remote` SPA shell (`src/main/routes/remote.ts` + `src/renderer/remote/`, hash-nav, WS status), `next=/remote/` support in browser login (allowlisted, no open redirect). Appliance behavior: no windows at boot, app survives window-all-closed, quit via tray; settings window auto-opens on EADDRINUSE (`listen()` now rejects on error). Forge entries added: `remote`, `settings` (with preload). Tests: 216 passing (was 202); new `tests/app-settings.test.ts`, `tests/remote-routes.test.ts`. `electron-forge package` succeeds; GUI not launchable here (no X server) — tray/settings window need a manual check on the Mac mini. |
| 2 — Slides (V2-C UI, notes, thumbnails, A/B, offline) | ✅ done 2026-06-10 (code) / ⚠️ needs live verify | `SlidesState` extended (notes, notesOpen, thumbnails, backup deck, offlineMode/cacheWarmed, contentKind, deckUrl) via `makeSlidesState()`. GSC compat: `src/main/services/gsc-status.ts` (flat fields merged into now-unauthenticated GET /api/status) + `src/main/routes/gsc-compat.ts` (open-presentation, next/previous-slide, go-to-slide 1-based, reload/close, open-slido→url mode, honest 400s for unsupported endpoints) — cookie-less, IP-allowlist-gated like GSC. Native API additions: GET /api/slides/status, GET /api/slides/thumbnails, POST /api/slides/load {backupUrl}, POST /api/slides/offline-mode. Window manager rebuilt with GSC techniques: S-key spawns Google presenter-notes popup (hidden capture source), 1 Hz DOM poll (aria-posinset/setsize, title, `.punch-viewer-speakernotes-text-body-scrollable`, U+FFFD normalize), arrow-key navigation + hash jump, capturePage thumbnails, backup deck preload into inactive A/B instance, 30s cache-warm timer. /remote slides page: V2-C-adapted UI (counter, thumb strip, notes card w/ zoom+readout+localStorage, 72px prev/next, goto, A/B switch, offline toggle, haptics, keyboard shortcuts). Tests 235 passing (19 new in `tests/gsc-compat.test.ts`); logout test now uses /api/instance-status (status is unauthenticated). **Manual verify on Mac mini:** presenter-popup capture, navigation against real deck, GSC Companion module pointed at PConAir port. |
| 3 — Tunnel + QR + PIN | ✅ done 2026-06-10 (code) / ⚠️ needs live verify | `src/main/tunnel/manager.ts` (spawns cloudflared: quick tunnel parses trycloudflare URL, or token mode w/ custom domain; binary via PCONAIR_CLOUDFLARED_PATH > resources/cloudflared/<plat> > PATH). `TunnelState` added to AppState (enabled/status/url/pinRequired/lastError). PIN gate `src/main/security/tunnel-pin.ts`: tunnel clients detected by cf-* headers, every path gated by 4–8-digit PIN (bcrypt hash in app-settings.json), 5-failure lockout, 24h cookie. Routes `src/main/routes/tunnel.ts`: GET /api/tunnel/status + /api/qr (unauth), POST /api/tunnel + /api/tunnel/config (admin), /api/slides/share-link + /api/slides/show-qr (operator), GSC-compat /api/show-tunnel-qr + /api/hide-tunnel-qr. QR overlay window `src/main/tunnel/qr-overlay.ts` (ported from GSC). Web GUI: QR button in header (one tap → modal), tunnel section in settings page (status dot, domain/token/PIN config). `qrcode` dep added. 250 tests (15 new in tests/tunnel.test.ts). **Live verify:** real cloudflared spawn + phone through tunnel. Note: cloudflared binary not bundled yet — needs a download step like GSC's `yarn download:cloudflared` (phase 11 packaging item). |
| 4 — Lower thirds UI + FaireL3s themes | ✅ done 2026-06-10 | 13 FaireL3s styles bundled as built-in CSS themes: `src/main/l3/fairel3s-theme-convert.ts` (pure converter, geometry ported from generate_lowerthirds.py) + `fairel3s-styles-data.ts` (GENERATED from FaireL3s/style_*.json — regenerate with the node snippet that maps style files → FAIREL3S_STYLES; see phase-4 commit). Theme store now serves default + 14 built-ins via BUILT_INS. New endpoints: POST /api/l3/playlists/next + /prev (operator) — step active playlist with wrap, returns playlistPosition/playlistLength. /remote L3 page: cue gallery (tap select, double-tap take, live/selected highlights), Take/Clear 72px buttons, stacking toggle, playlist selector + next/prev, add-cue form (admin), CSV import. 256 tests (6 new). |
| 5 — Still store + slideshow | ✅ done 2026-06-10 | `SlideshowState` added to `MediaLibraryState` (running/paused/itemIds/position/intervalSec/transition cut|fade). Engine `src/main/media-library/slideshow.ts` (server-side interval, Electron-free, wraps, pause/resume/stop keeps current image on air). Route POST /api/media-library/slideshow {action: play|pause|resume|stop|next|prev}. /remote stills page: thumbnail gallery (tap = toggle slideshow membership w/ order badges, double-tap = take), take/clear, slideshow controls + position, multi-file upload (admin). Fade transition is honored by render pages (phase 6); Electron media window does hard cuts. 262 tests (6 new in tests/slideshow.test.ts). |
| 6 — /render/:type + bg/key modes | ✅ done 2026-06-10 | `renderOutputs` in AppState (per type: bg transparent/black/white/chroma/opaque, chromaColor, claimedOutput). `src/main/routes/render.ts`: GET /render/:type (self-contained HTML, no cookies, connects `/ws?render=1`, hydrates from snapshot, fade/cut for stills, theme-CSS L3, thumbnailCurrent for slides @1280px, url banner), GET /api/render/outputs, POST /api/render/:type/background + /output (claim; warns-not-blocks on conflict). WS: `?render=1` connections are cookie-less, IP-allowlist-checked, read-only (actions ignored). New unauth endpoints for OBS: GET /api/l3/themes/:name/css, GET /api/media-library/:id/download. `L3State.activeTheme` added. Web GUI: per-page "Output & key mode" card (bg selector, chroma picker, copy-OBS-URL, live status, claimed output). 273 tests (11 new). |
| 7 — Packages system | ✅ done 2026-06-10 | `src/main/packages/loader.ts` (manifest validation, schema→default state, dir scan w/ per-package error reporting) + `state-hub.ts` (registry + per-package state, namespace pub/sub, state survives rescan). Routes `src/main/routes/packages.ts`: GET/POST /api/packages(+/rescan, /:id/state), GET /packages/:id/render(/:renderId), /control, /assets/* (assets confined to assets/ subdir). WS: `{type:'subscribe', namespace:'package:<id>'}` on any connection (render or authed) → `{type:'state', namespace, state}` snapshot + live pushes. Mutations are cookie-less (LAN/IP-allowlist, same model as GSC compat) so Companion + control UIs share one path. packagesRoot = userData/packages (created at boot); server takes `packagesRoot` dep (tests use temp dirs). Web GUI packages page: list w/ LIVE chip (WS subscriber count), Open Control UI, per-render Copy OBS URL, rescan button, load-error display. 282 tests (9 new). Note for phase 8 bundled pkgs: initial AppState snapshot on render WS has `payload` (not `state`) — package render pages must guard `if (type==='state' && state)`. |
| 8 — Bundled packages (COURTVISION, News, FFG) | ✅ done 2026-06-11 | Repo-root `bundled-packages/{hoops,news,ffg}` shipped via forge `extraResource`; `createPackageHub` now takes `string | string[]` roots (bundled scanned first, duplicate ids error instead of shadowing); index.ts passes `[bundled, userData/packages]` (packaged: `process.resourcesPath/bundled-packages`, dev: `app.getAppPath()`). Each package has `assets/state.js` (`PConAirPackage.connect(id, onState)` → `/ws?render=1` + `{type:'subscribe', namespace:'package:<id>'}`, auto-reconnect 2s, guards the AppState `payload` snapshot; mutations via POST /api/packages/:id/state). **hoops**: render+control ported from obs-mcp prototype, CSS verbatim; clocks stored as deadlines (`clockEndsAt`/`shotEndsAt` epoch ms when running, static `clock`/`shotClock` when stopped) so reloads hydrate correctly; player card animates via `.in` class; netbug/ticker toggleable. **news**: bug/LIVE toggles, L3 take/clear (FaireL3s default style, dark variant), ticker items use `lead:: rest` for bold lead-ins. **ffg**: 5 renders per plan (single-pip ?team→state.activeTeam fallback, four-portrait, four-up, head-to-head ?slot→h2h.slotA/B, champion ?winner→state.winner+finalScore); assets = cardboard.css verbatim, overlay-kit.js (teams/localStorage stripped, icons → /packages/ffg/assets/icons/), ffg-common.js (`FFG.start(build, keyFn)` builds DOM on first state, reloads when teams/h2h/winner config changes — same semantics as original ffg.teams reload, scores/timer update live; ship-by timer = `timer.{running,remaining,endsAt}`); control.html is tabbed SCORES (timer, bumps, goal, reset, champion reveal) + SETUP (team/H2H editors ← old admin.html, OBS URL table). State shape follows plan §Bundled packages: `teams[].handle` (was `user`), `h2h.slotA/slotB` arrays. 287 tests (5 new: multi-root hub, bundled scan/renders/state-seed, HTTP serving). Live verify in OBS still pending (phase 11 e2e). |
| 9 — Companion module | ✅ done 2026-06-12 | Module v0.2.0 rewrite (`packages/companion-module-pconair/`, `ws` now a runtime dep): WS-first `client.ts` (`/ws?companion=1`, auto-reconnect, HTTP-polling fallback w/ operator PIN), `actions.ts` (GSC compat section — IDs match companion-module-gslide-opener exactly, unsupported ones labeled "(not supported)" and report errors gracefully — plus native slides/url/L3/stills/slideshow/A-B/mode/render-bg, ~50 actions), `variables.ts` (61 vars incl. every GSC flat field), `feedbacks.ts` (29 incl. all GSC feedback IDs), `presets.ts` (categories: Slides, A/B, Mode, Lower Thirds, Still Store, Status, Tunnel). **Dynamic package interfaces**: `packages.ts` polls GET /api/packages every 30s, re-registers defs when the list changes (no Companion restart); `pkg-engine.ts` interprets declarative `companionActions/Variables/Feedbacks` from package.json — ops `set|add|toggle|countdown_start/stop/reset` (deadline-based, 1s display tick), field paths w/ option interpolation, derived fields, mm:ss clock vars. All three bundled packages declare interfaces (hoops 23a/9v/9f, news 7/3/4, ffg 11/12/4). Server side: cookie-less `?companion=1` WS path (IP-allowlist at upgrade, cf-* tunnel headers refused, marks `connectionStatus.companionConnected`, action dispatch → `action_result`/`error` frames, package namespace subscribe on same socket); loader passes companion* fields through /api/packages; L3 playlist + stills take/clear extracted to `playlist-ops.ts`/`stills-ops.ts` so HTTP routes and the 34-action dispatcher share one path. Tests 329 passing (42 new: companion-actions, companion-pkg-engine, companion-ws). Session note: companion-ws tests originally timed out — server pushes the snapshot in the same tick the upgrade completes, so listeners attached after awaiting 'open' missed it; fixed by buffering messages from socket creation (test-side only). Live verify vs real Companion still pending (phase 11). |
| 10 — Stagetimer + URL mode + Timer page | pending | |
| 11 — Polish/hardening | pending | |

## Phase 2 research findings (GSC Companion compat surface)

The GSC Companion module (`companion-module-gslide-opener/`) uses **HTTP polling, no auth, no cookies** (GSC gates by IP allowlist only — matches v2 plan's API model). To keep existing buttons working, PConAir must serve:

- `GET /api/status` polled at 1 Hz. Module reads **flat fields**: `presentationOpen, notesOpen, currentSlide (1-based), totalSlides, presentationUrl, contentKind ('slides'|'slido'), slideInfo ("3 / 10"), isFirstSlide, isLastSlide, nextSlide, previousSlide, presentationTitle, timerElapsed, presentationDisplayId, notesDisplayId, loginState, loggedInUser, backupControlsEnabled, notesZoomSteps, notesZoomDefault, notesLayout, perfectcue {enabled, ports[]}`. **Merge these into PConAir's existing /api/status response** (extra fields are harmless to both consumers).
- Action POSTs (all unauthenticated, IP-allowlist only): `/api/open-presentation {url}`, `/api/open-presentation-with-notes {url}`, `/api/open-slido {url}`, `/api/open-url {url, backgroundColor}`, `/api/open-key-fill`, `/api/close-key-fill`, `/api/open-preset {preset:1-3}`, `/api/close-presentation`, `/api/next-slide`, `/api/previous-slide`, `/api/go-to-slide {slide}` (1-based), `/api/reload-presentation`, `/api/toggle-video`, `/api/open-speaker-notes`, `/api/close-speaker-notes`, `/api/scroll-notes-down`, `/api/scroll-notes-up`, `/api/zoom-in-notes`, `/api/zoom-out-notes`, `/api/show-tunnel-qr {duration}`, `/api/hide-tunnel-qr`, `/api/set-backup-controls {enabled}`, `/api/preferences {notesLayout}`, `/api/relaunch-speaker-notes`, `/api/set-perfectcue-enabled`, `/api/toggle-perfectcue-port`. Success = HTTP 200 + JSON.
- Phase 2 must implement the slides/notes/url subset; tunnel-qr lands in phase 3; key-fill / perfectcue / toggle-video / backup-controls may return graceful no-op errors (module shows action failure but doesn't break) — decide per endpoint.
- GSC module action IDs / variables / feedbacks (for phase 9 module parity): variables incl. `current_slide, total_slides, slide_info, is_first/last_slide, presentation_title, content_kind, timer_elapsed…`; feedbacks `presentation_open, notes_open, on_slide, is_first_slide, is_last_slide, login_state, backup_controls_enabled, notes_layout_is`.
- V2-C design (`web-remote-v2c/plan.md`): Faire design tokens (§3), V2-C layout order: header (status dot + name + counter + toggles) → stagetimer card → slide strip (current/next thumbs) → notes card (toolbar: scroll/zoom + px readout, Lora 19/30) → prev/next 72px buttons → bottom tabs. Stagetimer tone tokens + overtime state. PConAir /remote already uses bottom-nav shell; adapt V2-C inside the Slides page.
- Current PConAir slides impl: `SlidesState {deckId, deckTitle, slideIndex (0-based), slideCount, isLoading}`; ops in `src/main/services/slide-ops.ts`; window manager `src/main/slides/window-manager.ts` (A/B windows, loads `/present`, navigates by aria-label click — weak). PConAir-native API: POST `/api/slides/load {deckUrl, instance}`, `/next`, `/prev`, `/goto {slideIndex 0-based}`, `/reload` — all operator-cookie-gated.

## Decisions log
- 2026-06-10: Branch `feat/v2-phase1-shell` created from `docs/phase3-phase4-complete` (carries the v2 plan doc). Commit at each phase boundary.
- Existing implementation status doc: `specs/11-implementation-status.md` (now 202 tests, doc says 167).
- Port default 8080, never 9595 (GSC's port).

## Reference sizes (measured, for budgeting)
- GSC `main.js`: 13,195 lines — section reads only
- v2c design: plan.md 400 ln, Web Remote UI.html 153 ln, design-canvas.jsx 253 ln + components/
- GSC companion module: ~1,230 ln JS
- Prototypes: hoops 208 ln, faire-nightly-news 133 ln
- FFG obs/: ~3,054 ln total
- PConAir src: 7,172 ln TS
