{
  "name": "@pconair/companion-module",
  "version": "0.2.0",
  "description": "Bitfocus Companion module for PC On Air",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "lint": "tsc --noEmit",
    "package": "npm run build && rm -rf pkg && mkdir -p pkg && cp -r companion dist package.json pkg/ && cd pkg && npm install --omit=dev && zip -r pconair-companion-0.2.0.zip . --exclude '*.zip' && cd .."
  },
  "dependencies": {
    "@companion-module/base": "^1.8.0",
    "ws": "^8"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/ws": "^8",
    "typescript": "^5.3.0"
  },
  "engines": {
    "node": ">=18"
  }
}
